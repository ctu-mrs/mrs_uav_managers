mrs_uav_managers:

  safety_area_manager:
  
    status_timer_rate: 10.0 # [Hz]

    scope_timer:

      enabled: false
      log_filename: "" # if empty, scope timers output to terminal; otherwise log to file
