rate: 25

estimator_types: [
  "OBJECT",
  "VIO",
  "ICP",
  "RTK",
  "OPTFLOWGPS",
  "GPS",
  "OPTFLOW",
  "OTHER",
]

gains: [
  "supersoft",
  "soft",
  "tight",
]

gain_management:

  # list of allowed gains per odometry mode
  allowed_gains:
    OBJECT: ["supersoft"]
    VIO: ["supersoft"]
    ICP: ["supersoft"]
    RTK: ["soft", "supersoft", "tight"]
    OPTFLOWGPS: ["soft", "supersoft"]
    GPS: ["soft", 'supersoft', 'tight']
    OPTFLOW: ["supersoft"]
    OTHER: ["supersoft"]

  # those gains will be used automatically when a localization mode switches
  # and the current gains are not in the allowed list (next paragraphs)
  fallback_gains:
    OBJECT: "supersoft"
    VIO: "supersoft"
    ICP: "supersoft"
    RTK: "tight"
    OPTFLOWGPS: "soft"
    GPS: "tight"
    OPTFLOW: "supersoft"
    OTHER: "supersoft"
